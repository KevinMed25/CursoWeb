const num1 = 20;
const num2 = "20"

console.log(parseInt(num2)) // parseInt() es una funcion -- funciona como un cast
console.log(num1.toString()) // toString() es un método,
// Si pasamos parametros o argumentos son funciones, si se usan con notación . es método